---
import ToolLayout from '../../layouts/ToolLayout.astro';
import { getToolBySlug } from '../../data/electricalTools';

const tool = getToolBySlug('inspection-report-template')!;

const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "What should an electrical inspection report include?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "A professional electrical inspection report should include panel identification (manufacturer, amperage rating, number of spaces, age), component-by-component ratings using a Pass/Monitor/Fail scale for the main panel, sub-panels, wiring, breakers, grounding, GFCI/AFCI protection, and service entrance, voltage and amperage measurements, insulation resistance and ground fault readings, a prioritized list of findings with estimated repair costs, an overall pass/fail assessment, and inspector credentials with license number."
      }
    },
    {
      "@type": "Question",
      "name": "How much does an electrical inspection cost?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "A standard electrical inspection costs between $100 and $250 depending on the property size, panel amperage, and scope. Pre-purchase inspections for real estate transactions typically run $200 to $400 since they require more detailed documentation including circuit-by-circuit testing. Annual maintenance inspections for commercial properties range from $200 to $600 depending on the number of panels and circuits."
      }
    },
    {
      "@type": "Question",
      "name": "What is checked during an electrical inspection?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "A thorough electrical inspection checks the main panel and sub-panels for proper sizing and condition, wiring for code compliance and damage, all breakers and circuits for proper loading, grounding and bonding systems, GFCI and AFCI protection in required locations, service entrance condition, conduit and junction boxes for safety issues, and outlet and switch operation. Detailed voltage, amperage, and resistance measurements are recorded for each area inspected."
      }
    },
    {
      "@type": "Question",
      "name": "How do I write a professional electrical inspection report?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Start with complete property and panel identification including manufacturer, amperage rating, and age. Rate each electrical component using a standardized scale such as Pass, Monitor, or Fail. Record all measurements including voltage readings, amperage draws, insulation resistance, and ground fault test results with actual values. List every finding with a recommended action, estimated cost, and priority level. Include an overall assessment and total repair cost estimate. Always add a disclaimer about the inspection scope and limitations."
      }
    }
  ]
};
---

<ToolLayout tool={tool} extraSchema={[faqSchema]}>
  <div id="inspection-report-form">

    <!-- Section: Header / Company Info -->
    <h3 class="font-heading font-bold text-lg mb-4 text-text-primary">Inspector &amp; Company Info</h3>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="company-name">Company Name</label>
        <input type="text" id="company-name" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="inspector-name">Inspector Name</label>
        <input type="text" id="inspector-name" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="inspector-phone">Phone</label>
        <input type="text" id="inspector-phone" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="license-number">License #</label>
        <input type="text" id="license-number" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="inspection-date">Inspection Date</label>
        <input type="date" id="inspection-date" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="report-number">Report #</label>
        <input type="text" id="report-number" value="IR-001" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="property-address">Property Address</label>
        <input type="text" id="property-address" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="inspection-reason">Reason for Inspection</label>
        <select id="inspection-reason" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors">
          <option value="Pre-Purchase">Pre-Purchase</option>
          <option value="Annual Inspection" selected>Annual Inspection</option>
          <option value="Insurance">Insurance</option>
          <option value="Warranty Claim">Warranty Claim</option>
          <option value="Complaint Investigation">Complaint Investigation</option>
        </select>
      </div>
    </div>

    <!-- Section: System Inventory -->
    <h3 class="font-heading font-bold text-lg mb-4 text-text-primary">System Inventory</h3>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="equip-type">Equipment Type</label>
        <select id="equip-type" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors">
          <option value="Main Panel">Main Panel</option>
          <option value="Sub-Panel">Sub-Panel</option>
          <option value="Circuit Breaker">Circuit Breaker</option>
          <option value="Wiring System">Wiring System</option>
          <option value="Outlets/Switches">Outlets/Switches</option>
          <option value="Whole House">Whole House</option>
        </select>
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="equip-make">Make</label>
        <input type="text" id="equip-make" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="equip-model">Model</label>
        <input type="text" id="equip-model" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="equip-serial">Serial #</label>
        <input type="text" id="equip-serial" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="install-date">Install Date</label>
        <input type="date" id="install-date" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2">Equipment Age</label>
        <div id="equip-age-display" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-lg font-bold text-text-muted">&mdash;</div>
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="remaining-life">Estimated Remaining Life (years)</label>
        <input type="number" id="remaining-life" min="0" max="50" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="overall-condition">Overall Condition</label>
        <select id="overall-condition" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors">
          <option value="Excellent">Excellent</option>
          <option value="Good" selected>Good</option>
          <option value="Fair">Fair</option>
          <option value="Poor">Poor</option>
          <option value="Failed">Failed</option>
        </select>
      </div>
    </div>

    <!-- Section: Component Ratings -->
    <h3 class="font-heading font-bold text-lg mb-4 text-text-primary">Component Ratings</h3>
    <div class="space-y-4 mb-8">
      <!-- Main Panel -->
      <div class="bg-bg-dark border border-border rounded-lg p-4">
        <div class="flex flex-col md:flex-row md:items-center gap-3 mb-2">
          <span class="font-medium text-text-primary w-48 shrink-0">Main Panel</span>
          <select class="component-rating bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary focus:border-accent focus:outline-none transition-colors" data-component="main-panel">
            <option value="Pass">Pass (Green)</option>
            <option value="Monitor">Monitor (Yellow)</option>
            <option value="Fail">Fail (Red)</option>
          </select>
          <span class="rating-indicator w-4 h-4 rounded-full bg-green-500 shrink-0" data-for="main-panel"></span>
        </div>
        <textarea class="component-notes w-full bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary text-sm focus:border-accent focus:outline-none transition-colors resize-y" data-component="main-panel" rows="2" placeholder="Notes..."></textarea>
      </div>
      <!-- Wiring -->
      <div class="bg-bg-dark border border-border rounded-lg p-4">
        <div class="flex flex-col md:flex-row md:items-center gap-3 mb-2">
          <span class="font-medium text-text-primary w-48 shrink-0">Wiring</span>
          <select class="component-rating bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary focus:border-accent focus:outline-none transition-colors" data-component="electrical">
            <option value="Pass">Pass (Green)</option>
            <option value="Monitor">Monitor (Yellow)</option>
            <option value="Fail">Fail (Red)</option>
          </select>
          <span class="rating-indicator w-4 h-4 rounded-full bg-green-500 shrink-0" data-for="electrical"></span>
        </div>
        <textarea class="component-notes w-full bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary text-sm focus:border-accent focus:outline-none transition-colors resize-y" data-component="electrical" rows="2" placeholder="Notes..."></textarea>
      </div>
      <!-- Circuit Breakers -->
      <div class="bg-bg-dark border border-border rounded-lg p-4">
        <div class="flex flex-col md:flex-row md:items-center gap-3 mb-2">
          <span class="font-medium text-text-primary w-48 shrink-0">Circuit Breakers</span>
          <select class="component-rating bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary focus:border-accent focus:outline-none transition-colors" data-component="breakers">
            <option value="Pass">Pass (Green)</option>
            <option value="Monitor">Monitor (Yellow)</option>
            <option value="Fail">Fail (Red)</option>
          </select>
          <span class="rating-indicator w-4 h-4 rounded-full bg-green-500 shrink-0" data-for="breakers"></span>
        </div>
        <textarea class="component-notes w-full bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary text-sm focus:border-accent focus:outline-none transition-colors resize-y" data-component="breakers" rows="2" placeholder="Notes..."></textarea>
      </div>
      <!-- Conduit & Raceways -->
      <div class="bg-bg-dark border border-border rounded-lg p-4">
        <div class="flex flex-col md:flex-row md:items-center gap-3 mb-2">
          <span class="font-medium text-text-primary w-48 shrink-0">Conduit &amp; Raceways</span>
          <select class="component-rating bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary focus:border-accent focus:outline-none transition-colors" data-component="conduit">
            <option value="Pass">Pass (Green)</option>
            <option value="Monitor">Monitor (Yellow)</option>
            <option value="Fail">Fail (Red)</option>
          </select>
          <span class="rating-indicator w-4 h-4 rounded-full bg-green-500 shrink-0" data-for="conduit"></span>
        </div>
        <textarea class="component-notes w-full bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary text-sm focus:border-accent focus:outline-none transition-colors resize-y" data-component="conduit" rows="2" placeholder="Notes..."></textarea>
      </div>
      <!-- Outlets/Switches -->
      <div class="bg-bg-dark border border-border rounded-lg p-4">
        <div class="flex flex-col md:flex-row md:items-center gap-3 mb-2">
          <span class="font-medium text-text-primary w-48 shrink-0">Outlets / Switches</span>
          <select class="component-rating bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary focus:border-accent focus:outline-none transition-colors" data-component="outlets">
            <option value="Pass">Pass (Green)</option>
            <option value="Monitor">Monitor (Yellow)</option>
            <option value="Fail">Fail (Red)</option>
          </select>
          <span class="rating-indicator w-4 h-4 rounded-full bg-green-500 shrink-0" data-for="outlets"></span>
        </div>
        <textarea class="component-notes w-full bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary text-sm focus:border-accent focus:outline-none transition-colors resize-y" data-component="outlets" rows="2" placeholder="Notes..."></textarea>
      </div>
      <!-- Grounding / Bonding -->
      <div class="bg-bg-dark border border-border rounded-lg p-4">
        <div class="flex flex-col md:flex-row md:items-center gap-3 mb-2">
          <span class="font-medium text-text-primary w-48 shrink-0">Grounding / Bonding</span>
          <select class="component-rating bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary focus:border-accent focus:outline-none transition-colors" data-component="filtration">
            <option value="Pass">Pass (Green)</option>
            <option value="Monitor">Monitor (Yellow)</option>
            <option value="Fail">Fail (Red)</option>
          </select>
          <span class="rating-indicator w-4 h-4 rounded-full bg-green-500 shrink-0" data-for="filtration"></span>
        </div>
        <textarea class="component-notes w-full bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary text-sm focus:border-accent focus:outline-none transition-colors resize-y" data-component="filtration" rows="2" placeholder="Notes..."></textarea>
      </div>
      <!-- Safety Controls -->
      <div class="bg-bg-dark border border-border rounded-lg p-4">
        <div class="flex flex-col md:flex-row md:items-center gap-3 mb-2">
          <span class="font-medium text-text-primary w-48 shrink-0">Safety Controls</span>
          <select class="component-rating bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary focus:border-accent focus:outline-none transition-colors" data-component="safety">
            <option value="Pass">Pass (Green)</option>
            <option value="Monitor">Monitor (Yellow)</option>
            <option value="Fail">Fail (Red)</option>
          </select>
          <span class="rating-indicator w-4 h-4 rounded-full bg-green-500 shrink-0" data-for="safety"></span>
        </div>
        <textarea class="component-notes w-full bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary text-sm focus:border-accent focus:outline-none transition-colors resize-y" data-component="safety" rows="2" placeholder="Notes..."></textarea>
      </div>
      <!-- GFCI / AFCI Protection -->
      <div class="bg-bg-dark border border-border rounded-lg p-4">
        <div class="flex flex-col md:flex-row md:items-center gap-3 mb-2">
          <span class="font-medium text-text-primary w-48 shrink-0">GFCI / AFCI Protection</span>
          <select class="component-rating bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary focus:border-accent focus:outline-none transition-colors" data-component="drainage">
            <option value="Pass">Pass (Green)</option>
            <option value="Monitor">Monitor (Yellow)</option>
            <option value="Fail">Fail (Red)</option>
          </select>
          <span class="rating-indicator w-4 h-4 rounded-full bg-green-500 shrink-0" data-for="drainage"></span>
        </div>
        <textarea class="component-notes w-full bg-bg-card border border-border rounded-lg px-4 py-2 text-text-primary text-sm focus:border-accent focus:outline-none transition-colors resize-y" data-component="drainage" rows="2" placeholder="Notes..."></textarea>
      </div>
    </div>

    <!-- Section: Measurements -->
    <h3 class="font-heading font-bold text-lg mb-4 text-text-primary">Measurements</h3>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="supply-temp">Line Voltage (V)</label>
        <input type="number" id="supply-temp" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="return-temp">Load Voltage (V)</label>
        <input type="number" id="return-temp" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2">Voltage Drop (V)</label>
        <div id="temp-split-display" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-lg font-bold text-text-muted">&mdash;</div>
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="co-reading">Ground Fault (mA)</label>
        <input type="number" id="co-reading" min="0" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2">Fault Level</label>
        <div id="co-level-display" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-lg font-bold text-text-muted">&mdash;</div>
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="gas-pressure">Amperage Draw (A)</label>
        <input type="number" id="gas-pressure" step="0.1" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="static-pressure">Insulation Resistance (M&Omega;)</label>
        <input type="number" id="static-pressure" step="0.01" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
      </div>
    </div>

    <!-- Section: Findings -->
    <h3 class="font-heading font-bold text-lg mb-4 text-text-primary">Findings</h3>
    <div class="mb-8">
      <div id="findings-table" class="space-y-3">
        <div class="finding-row bg-bg-dark border border-border rounded-lg p-4">
          <div class="grid grid-cols-1 md:grid-cols-[1fr_1fr_120px_140px_40px] gap-3 items-end">
            <div>
              <label class="block text-sm font-medium text-text-secondary mb-2">Description</label>
              <input type="text" class="finding-desc w-full bg-bg-card border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
            </div>
            <div>
              <label class="block text-sm font-medium text-text-secondary mb-2">Recommended Action</label>
              <input type="text" class="finding-action w-full bg-bg-card border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
            </div>
            <div>
              <label class="block text-sm font-medium text-text-secondary mb-2">Est. Cost ($)</label>
              <input type="number" min="0" step="1" class="finding-cost w-full bg-bg-card border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
            </div>
            <div>
              <label class="block text-sm font-medium text-text-secondary mb-2">Priority</label>
              <select class="finding-priority w-full bg-bg-card border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors">
                <option value="Immediate/Safety">Immediate / Safety</option>
                <option value="High">High</option>
                <option value="Medium" selected>Medium</option>
                <option value="Low">Low</option>
              </select>
            </div>
            <div>
              <label class="block text-sm font-medium text-text-secondary mb-2">&nbsp;</label>
              <button type="button" class="remove-finding-btn w-full bg-red-900/30 text-red-400 rounded-lg py-3 hover:bg-red-900/50 transition-colors cursor-pointer text-lg font-bold" title="Remove finding">&times;</button>
            </div>
          </div>
        </div>
      </div>
      <button type="button" id="add-finding-btn" class="mt-3 text-accent hover:text-accent-hover transition-colors cursor-pointer text-sm font-medium">+ Add Finding</button>
    </div>

    <!-- Section: Summary -->
    <h3 class="font-heading font-bold text-lg mb-4 text-text-primary">Summary</h3>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-6">
      <div class="bg-bg-dark rounded-xl p-5 text-center">
        <div class="text-text-muted text-xs font-medium uppercase tracking-wider mb-2">Good Condition</div>
        <div id="summary-good" class="font-heading font-extrabold text-3xl text-green-400">0</div>
      </div>
      <div class="bg-bg-dark rounded-xl p-5 text-center">
        <div class="text-text-muted text-xs font-medium uppercase tracking-wider mb-2">Needs Attention</div>
        <div id="summary-attention" class="font-heading font-extrabold text-3xl text-yellow-400">0</div>
      </div>
      <div class="bg-bg-dark rounded-xl p-5 text-center">
        <div class="text-text-muted text-xs font-medium uppercase tracking-wider mb-2">Total Repair Cost</div>
        <div id="summary-cost" class="font-heading font-extrabold text-3xl text-accent">$0</div>
      </div>
      <div class="bg-bg-dark rounded-xl p-5 text-center">
        <div class="text-text-muted text-xs font-medium uppercase tracking-wider mb-2">Overall Assessment</div>
        <div id="summary-assessment-display" class="font-heading font-extrabold text-xl text-text-primary">&mdash;</div>
      </div>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="overall-assessment">Overall Assessment</label>
        <select id="overall-assessment" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors">
          <option value="Pass">Pass</option>
          <option value="Conditional Pass" selected>Conditional Pass</option>
          <option value="Fail">Fail</option>
        </select>
      </div>
      <div>
        <label class="block text-sm font-medium text-text-secondary mb-2" for="additional-notes">Additional Notes</label>
        <textarea id="additional-notes" rows="3" class="w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors resize-y"></textarea>
      </div>
    </div>

    <!-- Generate Button -->
    <button id="generate-btn" class="w-full md:w-auto bg-accent text-white font-heading font-bold text-base uppercase tracking-wider px-8 py-3.5 rounded-lg hover:bg-accent-hover hover:-translate-y-0.5 hover:shadow-[0_4px_20px_rgba(255,107,43,0.3)] transition-all cursor-pointer">
      Generate Inspection Report &rarr;
    </button>
  </div>

  <!-- Content sections -->
  <Fragment slot="content">
    <div class="max-w-[1080px] mx-auto px-6 md:px-12 pb-16 space-y-12">
      <!-- How to Use -->
      <section>
        <h2 class="font-heading font-bold text-2xl mb-4">How to Use This Template</h2>
        <div class="text-text-secondary leading-relaxed space-y-3">
          <p><strong class="text-text-primary">1. Fill in inspector and property details.</strong> Enter your company name, license number, and the property address. The report number auto-generates as IR-001 — change it to match your numbering system. Select the reason for the inspection so the report context is clear from the start.</p>
          <p><strong class="text-text-primary">2. Identify the system and rate each component.</strong> Record the equipment make, model, and serial number from the data plate. The age calculates automatically from the install date. Then go through each component — main panel, wiring, breakers, grounding, outlets — and assign a Pass, Monitor, or Fail rating with notes explaining your assessment.</p>
          <p><strong class="text-text-primary">3. Record measurements and document findings.</strong> Enter all voltage, ground fault, amperage, and insulation resistance readings. For every issue you find, add a finding row with a description, recommended action, estimated repair cost, and priority level. The summary section auto-calculates totals as you go.</p>
          <p><strong class="text-text-primary">4. Generate the printable report.</strong> Select your overall assessment, add any additional notes, and hit Generate. The report opens in a new tab with professional formatting, color-coded rating grids, a findings table with priority flags, and a signature line — ready to print or save as PDF.</p>
        </div>
      </section>

      <!-- What Makes a Professional Report -->
      <section>
        <h2 class="font-heading font-bold text-2xl mb-4">What Makes a Professional Electrical Inspection Report</h2>
        <div class="text-text-secondary leading-relaxed space-y-3">
          <p>A professional electrical inspection report does more than check boxes. Every section serves a specific purpose in communicating system condition to the property owner, buyer, insurer, or code authority.</p>
          <p><strong class="text-text-primary">Panel identification</strong> anchors the report to specific equipment. Manufacturer, amperage rating, number of spaces, and install date let anyone verify what was inspected. The calculated age gives the reader immediate context about whether the panel is nearing the end of its service life or has known recall issues.</p>
          <p><strong class="text-text-primary">Component ratings</strong> using a standardized Pass/Monitor/Fail scale make complex technical findings accessible to non-technical readers. A homebuyer doesn't need to understand double-tapped breakers or improper bonding — they need to know the panel passed, the grounding needs monitoring, and the GFCI protection failed. The notes behind each rating provide the technical detail for other electricians.</p>
          <p><strong class="text-text-primary">Measurements</strong> document the system's actual operating condition with verifiable numbers. Voltage readings, amperage draws on loaded circuits, insulation resistance values, and ground fault test results are objective data points that cannot be disputed. They create a baseline for future comparisons and protect the inspector if conditions change after the inspection.</p>
          <p><strong class="text-text-primary">Prioritized findings</strong> turn observations into actionable recommendations. Each finding includes what's wrong, what should be done, how much it will cost, and how urgent it is. This structure helps property owners make informed decisions about repairs and budgeting.</p>
          <p><strong class="text-text-primary">Documentation matters</strong> because inspection reports become legal documents. They're referenced in real estate transactions, insurance claims, code compliance disputes, and liability cases. A thorough, well-structured report protects your client, protects your business, and demonstrates the professionalism that separates serious electrical contractors from everyone else.</p>
        </div>
      </section>

      <!-- When To Use This -->
      <section>
        <h2 class="font-heading font-bold text-2xl mb-4">When To Use This</h2>
        <div class="text-text-secondary leading-relaxed space-y-3">
          <p><strong class="text-text-primary">Pre-purchase inspections.</strong> A buyer's agent or homebuyer hires you to evaluate the Electrical system before closing on a property. The report becomes part of the due diligence package. A clear pass/fail assessment with estimated repair costs gives the buyer leverage to negotiate price reductions or request repairs before closing. This is the highest-stakes use case — your report directly influences a six-figure transaction.</p>
          <p><strong class="text-text-primary">Annual maintenance inspections.</strong> During seasonal maintenance visits and maintenance visits, a formal inspection report documents the system's current condition and creates a year-over-year record. When a component rating moves from green to yellow, you can point to the history and recommend proactive replacement before it fails. This builds trust, generates future work, and protects you from customers who claim you never told them something was wearing out.</p>
          <p><strong class="text-text-primary">Insurance and warranty claims.</strong> Insurance companies require documented proof of system condition when processing claims for equipment damage. Warranty providers need inspection records showing proper maintenance was performed. A professional report with component ratings, measurements, and findings gives adjusters and warranty administrators exactly what they need to approve claims — without the back-and-forth of incomplete documentation.</p>
        </div>
      </section>

      <!-- FAQ -->
      <section>
        <h2 class="font-heading font-bold text-2xl mb-4">Frequently Asked Questions</h2>
        <div class="space-y-4">
          <details class="bg-bg-card border border-border rounded-lg">
            <summary class="px-5 py-4 cursor-pointer text-text-primary font-medium hover:text-accent transition-colors">What should an electrical inspection report include?</summary>
            <div class="px-5 pb-4 text-text-secondary text-sm leading-relaxed">A professional electrical inspection report should include panel identification (manufacturer, amperage rating, number of spaces, age), component-by-component ratings using a Pass/Monitor/Fail scale for the main panel, sub-panels, wiring, breakers, grounding, GFCI/AFCI protection, and service entrance, voltage and amperage measurements, insulation resistance and ground fault readings, a prioritized list of findings with estimated repair costs, an overall pass/fail assessment, and inspector credentials with license number.</div>
          </details>
          <details class="bg-bg-card border border-border rounded-lg">
            <summary class="px-5 py-4 cursor-pointer text-text-primary font-medium hover:text-accent transition-colors">How much does an electrical inspection cost?</summary>
            <div class="px-5 pb-4 text-text-secondary text-sm leading-relaxed">A standard electrical inspection costs between $100 and $250 depending on the property size, panel amperage, and scope. Pre-purchase inspections for real estate transactions typically run $200 to $400 since they require more detailed documentation including circuit-by-circuit testing. Annual maintenance inspections for commercial properties range from $200 to $600 depending on the number of panels and circuits.</div>
          </details>
          <details class="bg-bg-card border border-border rounded-lg">
            <summary class="px-5 py-4 cursor-pointer text-text-primary font-medium hover:text-accent transition-colors">What is checked during an electrical inspection?</summary>
            <div class="px-5 pb-4 text-text-secondary text-sm leading-relaxed">A thorough electrical inspection checks the main panel and sub-panels for proper sizing and condition, wiring for code compliance and damage, all breakers and circuits for proper loading, grounding and bonding systems, GFCI and AFCI protection in required locations, service entrance condition, conduit and junction boxes for safety issues, and outlet and switch operation. Detailed voltage, amperage, and resistance measurements are recorded for each area inspected.</div>
          </details>
          <details class="bg-bg-card border border-border rounded-lg">
            <summary class="px-5 py-4 cursor-pointer text-text-primary font-medium hover:text-accent transition-colors">How do I write a professional electrical inspection report?</summary>
            <div class="px-5 pb-4 text-text-secondary text-sm leading-relaxed">Start with complete property and panel identification including manufacturer, amperage rating, and age. Rate each electrical component using a standardized scale such as Pass, Monitor, or Fail. Record all measurements including voltage readings, amperage draws, insulation resistance, and ground fault test results with actual values. List every finding with a recommended action, estimated cost, and priority level. Include an overall assessment and total repair cost estimate. Always add a disclaimer about the inspection scope and limitations.</div>
          </details>
        </div>
      </section>
    </div>
  </Fragment>
</ToolLayout>

<script>
  // --- Default date ---
  const now = new Date();
  const dateInput = document.getElementById('inspection-date') as HTMLInputElement;
  if (dateInput && !dateInput.value) {
    dateInput.value = now.toISOString().slice(0, 10);
  }

  // --- Equipment Age auto-calculation ---
  function updateEquipAge() {
    const installDate = (document.getElementById('install-date') as HTMLInputElement).value;
    const display = document.getElementById('equip-age-display')!;
    if (installDate) {
      const install = new Date(installDate);
      const today = new Date();
      let years = today.getFullYear() - install.getFullYear();
      const monthDiff = today.getMonth() - install.getMonth();
      if (monthDiff < 0 || (monthDiff === 0 && today.getDate() < install.getDate())) {
        years--;
      }
      display.textContent = years + (years === 1 ? ' year' : ' years');
      display.className = 'w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-lg font-bold text-text-primary';
    } else {
      display.textContent = '\u2014';
      display.className = 'w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-lg font-bold text-text-muted';
    }
  }
  document.getElementById('install-date')?.addEventListener('input', updateEquipAge);

  // --- Overall Condition color coding ---
  function updateConditionColor() {
    const select = document.getElementById('overall-condition') as HTMLSelectElement;
    const val = select.value;
    select.classList.remove('text-green-400', 'text-yellow-400', 'text-red-400', 'text-text-primary');
    if (val === 'Excellent' || val === 'Good') {
      select.classList.add('text-green-400');
    } else if (val === 'Fair') {
      select.classList.add('text-yellow-400');
    } else if (val === 'Poor' || val === 'Failed') {
      select.classList.add('text-red-400');
    }
  }
  document.getElementById('overall-condition')?.addEventListener('change', updateConditionColor);
  updateConditionColor();

  // --- Component Rating color indicators ---
  function updateRatingIndicators() {
    document.querySelectorAll('.component-rating').forEach(sel => {
      const select = sel as HTMLSelectElement;
      const component = select.dataset.component;
      const indicator = document.querySelector(`.rating-indicator[data-for="${component}"]`) as HTMLElement;
      if (!indicator) return;
      indicator.classList.remove('bg-green-500', 'bg-yellow-500', 'bg-red-500');
      if (select.value === 'Pass') {
        indicator.classList.add('bg-green-500');
      } else if (select.value === 'Monitor') {
        indicator.classList.add('bg-yellow-500');
      } else {
        indicator.classList.add('bg-red-500');
      }
    });
    updateSummary();
  }
  document.querySelectorAll('.component-rating').forEach(el => {
    el.addEventListener('change', updateRatingIndicators);
  });

  // --- Temperature Split auto-calculation ---
  function updateTempSplit() {
    const supply = parseFloat((document.getElementById('supply-temp') as HTMLInputElement).value);
    const ret = parseFloat((document.getElementById('return-temp') as HTMLInputElement).value);
    const display = document.getElementById('temp-split-display')!;
    if (!isNaN(supply) && !isNaN(ret)) {
      const split = ret - supply;
      display.textContent = split.toFixed(1) + '\u00B0F';
      if (split >= 15 && split <= 20) {
        display.className = 'w-full bg-bg-dark border border-green-500/50 rounded-lg px-4 py-3 text-lg font-bold text-green-400';
      } else {
        display.className = 'w-full bg-bg-dark border border-yellow-500/50 rounded-lg px-4 py-3 text-lg font-bold text-yellow-400';
      }
    } else {
      display.textContent = '\u2014';
      display.className = 'w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-lg font-bold text-text-muted';
    }
  }
  document.getElementById('supply-temp')?.addEventListener('input', updateTempSplit);
  document.getElementById('return-temp')?.addEventListener('input', updateTempSplit);

  // --- CO Level indicator ---
  function updateCOLevel() {
    const co = parseFloat((document.getElementById('co-reading') as HTMLInputElement).value);
    const display = document.getElementById('co-level-display')!;
    if (!isNaN(co) && co >= 0) {
      if (co < 9) {
        display.textContent = co + ' PPM \u2014 Normal';
        display.className = 'w-full bg-bg-dark border border-green-500/50 rounded-lg px-4 py-3 text-lg font-bold text-green-400';
      } else if (co <= 35) {
        display.textContent = co + ' PPM \u2014 Elevated';
        display.className = 'w-full bg-bg-dark border border-yellow-500/50 rounded-lg px-4 py-3 text-lg font-bold text-yellow-400';
      } else {
        display.textContent = co + ' PPM \u2014 DANGER';
        display.className = 'w-full bg-bg-dark border border-red-500/50 rounded-lg px-4 py-3 text-lg font-bold text-red-400';
      }
    } else {
      display.textContent = '\u2014';
      display.className = 'w-full bg-bg-dark border border-border rounded-lg px-4 py-3 text-lg font-bold text-text-muted';
    }
  }
  document.getElementById('co-reading')?.addEventListener('input', updateCOLevel);

  // --- Dynamic Findings Table ---
  function addFindingRow() {
    const table = document.getElementById('findings-table')!;
    const row = document.createElement('div');
    row.className = 'finding-row bg-bg-dark border border-border rounded-lg p-4';
    row.innerHTML = `
      <div class="grid grid-cols-1 md:grid-cols-[1fr_1fr_120px_140px_40px] gap-3 items-end">
        <div>
          <input type="text" class="finding-desc w-full bg-bg-card border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
        </div>
        <div>
          <input type="text" class="finding-action w-full bg-bg-card border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
        </div>
        <div>
          <input type="number" min="0" step="1" class="finding-cost w-full bg-bg-card border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors" />
        </div>
        <div>
          <select class="finding-priority w-full bg-bg-card border border-border rounded-lg px-4 py-3 text-text-primary text-lg focus:border-accent focus:outline-none transition-colors">
            <option value="Immediate/Safety">Immediate / Safety</option>
            <option value="High">High</option>
            <option value="Medium" selected>Medium</option>
            <option value="Low">Low</option>
          </select>
        </div>
        <div>
          <button type="button" class="remove-finding-btn w-full bg-red-900/30 text-red-400 rounded-lg py-3 hover:bg-red-900/50 transition-colors cursor-pointer text-lg font-bold" title="Remove finding">&times;</button>
        </div>
      </div>
    `;
    table.appendChild(row);
    attachFindingListeners();
  }

  document.getElementById('add-finding-btn')?.addEventListener('click', addFindingRow);

  document.getElementById('findings-table')?.addEventListener('click', (e) => {
    const target = e.target as HTMLElement;
    if (target.classList.contains('remove-finding-btn')) {
      const row = target.closest('.finding-row');
      const allRows = document.querySelectorAll('.finding-row');
      if (allRows.length > 1 && row) {
        row.remove();
        updateSummary();
      }
    }
  });

  function attachFindingListeners() {
    document.querySelectorAll('.finding-cost').forEach(el => {
      el.removeEventListener('input', updateSummary);
      el.addEventListener('input', updateSummary);
    });
  }
  attachFindingListeners();

  // --- Summary auto-calculation ---
  function updateSummary() {
    // Count component ratings
    let good = 0;
    let attention = 0;
    document.querySelectorAll('.component-rating').forEach(sel => {
      const val = (sel as HTMLSelectElement).value;
      if (val === 'Pass') good++;
      else attention++;
    });
    document.getElementById('summary-good')!.textContent = String(good);
    document.getElementById('summary-attention')!.textContent = String(attention);

    // Sum finding costs
    let totalCost = 0;
    document.querySelectorAll('.finding-cost').forEach(el => {
      const val = parseFloat((el as HTMLInputElement).value);
      if (!isNaN(val)) totalCost += val;
    });
    document.getElementById('summary-cost')!.textContent = '$' + totalCost.toLocaleString();

    // Overall assessment display
    const assessment = (document.getElementById('overall-assessment') as HTMLSelectElement).value;
    const assessDisplay = document.getElementById('summary-assessment-display')!;
    assessDisplay.textContent = assessment;
    assessDisplay.classList.remove('text-green-400', 'text-yellow-400', 'text-red-400', 'text-text-primary');
    if (assessment === 'Pass') {
      assessDisplay.classList.add('text-green-400');
    } else if (assessment === 'Conditional Pass') {
      assessDisplay.classList.add('text-yellow-400');
    } else {
      assessDisplay.classList.add('text-red-400');
    }
  }
  document.getElementById('overall-assessment')?.addEventListener('change', updateSummary);
  updateSummary();

  // --- Helper ---
  function getVal(id: string): string {
    const el = document.getElementById(id) as HTMLInputElement | HTMLSelectElement | HTMLTextAreaElement | null;
    return el ? el.value : '';
  }

  // --- Generate Inspection Report ---
  function generateReport() {
    // Collect component ratings
    const components = [
      { key: 'main-panel', label: 'Main Panel' },
      { key: 'electrical', label: 'Electrical System' },
      { key: 'breakers', label: 'Circuit Breakers' },
      { key: 'conduit', label: 'Conduit & Raceways' },
      { key: 'outlets', label: 'Outlets / Switches' },
      { key: 'filtration', label: 'Grounding / Bonding' },
      { key: 'safety', label: 'Safety Controls' },
      { key: 'drainage', label: 'Drainage System' },
    ];

    let ratingsHtml = '';
    let greenCount = 0;
    let yellowCount = 0;
    let redCount = 0;
    components.forEach(comp => {
      const rating = (document.querySelector(`.component-rating[data-component="${comp.key}"]`) as HTMLSelectElement)?.value || 'Pass';
      const notes = (document.querySelector(`.component-notes[data-component="${comp.key}"]`) as HTMLTextAreaElement)?.value || '';
      let color = '#16a34a';
      let bg = '#dcfce7';
      let label = 'PASS';
      if (rating === 'Monitor') {
        color = '#ca8a04'; bg = '#fef9c3'; label = 'MONITOR'; yellowCount++;
      } else if (rating === 'Fail') {
        color = '#dc2626'; bg = '#fee2e2'; label = 'FAIL'; redCount++;
      } else {
        greenCount++;
      }
      ratingsHtml += `<tr>
        <td style="border:1px solid #ddd;padding:8px 12px;font-weight:600;">${comp.label}</td>
        <td style="border:1px solid #ddd;padding:8px 12px;text-align:center;"><span style="display:inline-block;padding:2px 12px;border-radius:4px;background:${bg};color:${color};font-weight:700;font-size:12px;text-transform:uppercase;">${label}</span></td>
        <td style="border:1px solid #ddd;padding:8px 12px;font-size:13px;color:#555;">${notes || '\u2014'}</td>
      </tr>`;
    });

    // Equipment age
    const installDate = getVal('install-date');
    let equipAge = '\u2014';
    if (installDate) {
      const install = new Date(installDate);
      const today = new Date();
      let years = today.getFullYear() - install.getFullYear();
      const md = today.getMonth() - install.getMonth();
      if (md < 0 || (md === 0 && today.getDate() < install.getDate())) years--;
      equipAge = years + (years === 1 ? ' year' : ' years');
    }

    // Temperature split
    const supplyT = parseFloat(getVal('supply-temp'));
    const returnT = parseFloat(getVal('return-temp'));
    let splitVal = '\u2014';
    let splitColor = '#666';
    if (!isNaN(supplyT) && !isNaN(returnT)) {
      const s = returnT - supplyT;
      splitVal = s.toFixed(1) + '\u00B0F';
      splitColor = (s >= 15 && s <= 20) ? '#16a34a' : '#ca8a04';
    }

    // CO Level
    const coVal = parseFloat(getVal('co-reading'));
    let coDisplay = '\u2014';
    let coColor = '#666';
    if (!isNaN(coVal) && coVal >= 0) {
      if (coVal < 9) { coDisplay = coVal + ' PPM'; coColor = '#16a34a'; }
      else if (coVal <= 35) { coDisplay = coVal + ' PPM'; coColor = '#ca8a04'; }
      else { coDisplay = coVal + ' PPM'; coColor = '#dc2626'; }
    }

    // Findings
    const findingRows = document.querySelectorAll('.finding-row');
    let findingsHtml = '';
    let totalCost = 0;
    findingRows.forEach(row => {
      const desc = (row.querySelector('.finding-desc') as HTMLInputElement)?.value || '';
      const action = (row.querySelector('.finding-action') as HTMLInputElement)?.value || '';
      const cost = parseFloat((row.querySelector('.finding-cost') as HTMLInputElement)?.value) || 0;
      const priority = (row.querySelector('.finding-priority') as HTMLSelectElement)?.value || 'Medium';
      if (desc || action) {
        totalCost += cost;
        let priBg = '#e0f2e9'; let priColor = '#166534';
        if (priority === 'Immediate/Safety') { priBg = '#fee2e2'; priColor = '#991b1b'; }
        else if (priority === 'High') { priBg = '#fef3c7'; priColor = '#92400e'; }
        else if (priority === 'Medium') { priBg = '#dbeafe'; priColor = '#1e40af'; }
        findingsHtml += `<tr>
          <td style="border:1px solid #ddd;padding:8px 12px;">${desc}</td>
          <td style="border:1px solid #ddd;padding:8px 12px;">${action}</td>
          <td style="border:1px solid #ddd;padding:8px 12px;text-align:right;">$${cost.toLocaleString()}</td>
          <td style="border:1px solid #ddd;padding:8px 12px;text-align:center;"><span style="display:inline-block;padding:2px 10px;border-radius:4px;background:${priBg};color:${priColor};font-weight:600;font-size:11px;text-transform:uppercase;">${priority}</span></td>
        </tr>`;
      }
    });

    // Overall condition color
    const condition = getVal('overall-condition');
    let condColor = '#16a34a';
    if (condition === 'Fair') condColor = '#ca8a04';
    else if (condition === 'Poor' || condition === 'Failed') condColor = '#dc2626';

    // Overall assessment color
    const assessment = getVal('overall-assessment');
    let assessColor = '#16a34a';
    let assessBg = '#dcfce7';
    if (assessment === 'Conditional Pass') { assessColor = '#ca8a04'; assessBg = '#fef9c3'; }
    else if (assessment === 'Fail') { assessColor = '#dc2626'; assessBg = '#fee2e2'; }

    const html = `<!DOCTYPE html>
<html><head><meta charset="UTF-8"><title>Electrical Inspection Report ${getVal('report-number')}</title>
<style>
  * { margin:0; padding:0; box-sizing:border-box; }
  body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; color:#1a1a1a; padding:40px; max-width:850px; margin:0 auto; font-size:14px; line-height:1.5; }
  h1 { font-size:22px; margin-bottom:4px; }
  h2 { font-size:15px; color:#333; margin:24px 0 8px; padding-bottom:4px; border-bottom:2px solid #e5e5e5; text-transform:uppercase; letter-spacing:0.5px; }
  .header { display:flex; justify-content:space-between; align-items:flex-start; margin-bottom:20px; padding-bottom:16px; border-bottom:3px solid #1a1a1a; }
  .header-left { }
  .header-right { text-align:right; font-size:13px; color:#555; }
  .grid2 { display:grid; grid-template-columns:1fr 1fr; gap:4px 24px; margin-bottom:6px; }
  .grid3 { display:grid; grid-template-columns:1fr 1fr 1fr; gap:4px 24px; margin-bottom:6px; }
  .label { font-weight:600; font-size:12px; color:#666; text-transform:uppercase; letter-spacing:0.3px; }
  .value { font-size:14px; margin-bottom:6px; }
  table { width:100%; border-collapse:collapse; margin-bottom:8px; }
  th { background:#f0f0f0; border:1px solid #ddd; padding:8px 12px; text-align:left; font-size:12px; text-transform:uppercase; letter-spacing:0.3px; }
  td { border:1px solid #ddd; padding:8px 12px; }
  .summary-grid { display:grid; grid-template-columns:1fr 1fr 1fr 1fr; gap:12px; margin:16px 0; }
  .summary-box { border:2px solid #e5e5e5; border-radius:8px; padding:16px; text-align:center; }
  .summary-box .num { font-size:28px; font-weight:800; }
  .summary-box .lbl { font-size:11px; color:#666; text-transform:uppercase; letter-spacing:0.5px; margin-top:4px; }
  .assess-badge { display:inline-block; padding:6px 20px; border-radius:6px; font-size:16px; font-weight:700; text-transform:uppercase; letter-spacing:1px; }
  .disclaimer { margin-top:30px; padding:16px; background:#f9f9f9; border-radius:6px; font-size:12px; color:#666; line-height:1.6; }
  .signature-block { display:grid; grid-template-columns:1fr 1fr; gap:40px; margin-top:40px; padding-top:16px; }
  .sig-line { border-top:1px solid #1a1a1a; padding-top:6px; font-size:12px; color:#666; text-transform:uppercase; }
  .sig-name { font-size:16px; font-weight:600; min-height:20px; }
  @media print { body { padding:20px; } }
</style>
</head><body>

<div class="header">
  <div class="header-left">
    <h1>Electrical Inspection Report</h1>
    <div style="font-size:13px;color:#555;">${getVal('company-name') || 'Electrical Inspection Services'}</div>
  </div>
  <div class="header-right">
    <div style="font-size:20px;font-weight:700;">${getVal('report-number')}</div>
    <div>${getVal('inspection-date')}</div>
    <div>${getVal('inspection-reason')}</div>
  </div>
</div>

<h2>Inspector Details</h2>
<div class="grid2">
  <div><span class="label">Inspector:</span></div><div><span class="label">Phone:</span></div>
  <div class="value">${getVal('inspector-name') || '\u2014'}</div><div class="value">${getVal('inspector-phone') || '\u2014'}</div>
  <div><span class="label">License #:</span></div><div><span class="label">Property Address:</span></div>
  <div class="value">${getVal('license-number') || '\u2014'}</div><div class="value">${getVal('property-address') || '\u2014'}</div>
</div>

<h2>System Identification</h2>
<div class="grid3">
  <div><span class="label">Equipment Type:</span></div><div><span class="label">Make:</span></div><div><span class="label">Model:</span></div>
  <div class="value">${getVal('equip-type')}</div><div class="value">${getVal('equip-make') || '\u2014'}</div><div class="value">${getVal('equip-model') || '\u2014'}</div>
  <div><span class="label">Serial #:</span></div><div><span class="label">Install Date:</span></div><div><span class="label">Equipment Age:</span></div>
  <div class="value">${getVal('equip-serial') || '\u2014'}</div><div class="value">${getVal('install-date') || '\u2014'}</div><div class="value">${equipAge}</div>
  <div><span class="label">Remaining Life:</span></div><div><span class="label">Overall Condition:</span></div><div></div>
  <div class="value">${getVal('remaining-life') ? getVal('remaining-life') + ' years' : '\u2014'}</div><div class="value" style="color:${condColor};font-weight:700;">${condition}</div><div></div>
</div>

<h2>Component Ratings</h2>
<table>
  <thead><tr><th>Component</th><th style="text-align:center;">Rating</th><th>Notes</th></tr></thead>
  <tbody>${ratingsHtml}</tbody>
</table>

<h2>Measurements</h2>
<table>
  <thead><tr><th>Supply Air</th><th>Return Air</th><th>Temp Split</th><th>CO Reading</th><th>Gas Pressure</th><th>Static Pressure</th></tr></thead>
  <tbody><tr>
    <td style="text-align:center;">${getVal('supply-temp') ? getVal('supply-temp') + '\u00B0F' : '\u2014'}</td>
    <td style="text-align:center;">${getVal('return-temp') ? getVal('return-temp') + '\u00B0F' : '\u2014'}</td>
    <td style="text-align:center;font-weight:700;color:${splitColor};">${splitVal}</td>
    <td style="text-align:center;font-weight:700;color:${coColor};">${coDisplay}</td>
    <td style="text-align:center;">${getVal('gas-pressure') ? getVal('gas-pressure') + '"WC' : '\u2014'}</td>
    <td style="text-align:center;">${getVal('static-pressure') ? getVal('static-pressure') + '"WC' : '\u2014'}</td>
  </tr></tbody>
</table>

${findingsHtml ? `<h2>Findings &amp; Recommendations</h2>
<table>
  <thead><tr><th>Description</th><th>Recommended Action</th><th style="text-align:right;">Est. Cost</th><th style="text-align:center;">Priority</th></tr></thead>
  <tbody>${findingsHtml}</tbody>
</table>` : ''}

<h2>Summary</h2>
<div class="summary-grid">
  <div class="summary-box">
    <div class="num" style="color:#16a34a;">${greenCount}</div>
    <div class="lbl">Good Condition</div>
  </div>
  <div class="summary-box">
    <div class="num" style="color:#ca8a04;">${yellowCount + redCount}</div>
    <div class="lbl">Needs Attention</div>
  </div>
  <div class="summary-box">
    <div class="num" style="color:#ea580c;">$${totalCost.toLocaleString()}</div>
    <div class="lbl">Total Repair Cost</div>
  </div>
  <div class="summary-box">
    <div class="assess-badge" style="background:${assessBg};color:${assessColor};">${assessment}</div>
    <div class="lbl" style="margin-top:8px;">Overall Assessment</div>
  </div>
</div>

${getVal('additional-notes') ? `<div style="margin-top:12px;"><span class="label">Additional Notes:</span><div class="value" style="background:#f9f9f9;padding:10px;border-radius:4px;white-space:pre-wrap;margin-top:4px;">${getVal('additional-notes')}</div></div>` : ''}

<div class="disclaimer">
  <strong>Disclaimer:</strong> This inspection report reflects the condition of the Electrical system at the time of inspection based on visual examination and standard diagnostic measurements. Hidden defects, inaccessible components, and conditions that may develop after the inspection date are not covered. This report is not a warranty or guarantee of future performance. The inspector and company assume no liability for conditions that were not observable or testable at the time of inspection. Repair cost estimates are approximate and may vary based on contractor pricing, parts availability, and actual scope of work.
</div>

<div class="signature-block">
  <div>
    <div class="sig-name">${getVal('inspector-name') || ''}</div>
    <div class="sig-line">Inspector Signature / Date</div>
  </div>
  <div>
    <div class="sig-name">&nbsp;</div>
    <div class="sig-line">Property Owner / Recipient</div>
  </div>
</div>

</body></html>`;

    const printWindow = window.open('', '_blank');
    if (printWindow) {
      printWindow.document.write(html);
      printWindow.document.close();
    }
  }

  document.getElementById('generate-btn')?.addEventListener('click', generateReport);
</script>
